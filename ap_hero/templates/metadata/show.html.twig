{% extends 'base.html.twig' %}

{% block title %}Metadata{% endblock %}

{% block body %}
    <h1>Metadata</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ metadata.id }}</td>
            </tr>
            <tr>
                <th>Phone_number</th>
                <td>{{ metadata.phoneNumber }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ metadata.type }}</td>
            </tr>
            <tr>
                <th>Field</th>
                <td>{{ metadata.field }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('metadata_index') }}">back to list</a>

    <a href="{{ path('metadata_edit', {'id': metadata.id}) }}">edit</a>

    {{ include('metadata/_delete_form.html.twig') }}
{% endblock %}
