{% extends 'base.html.twig' %}

{% block title %}CartItem index{% endblock %}

{% block body %}
    <h1>CartItem index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Product</th>
                <th>Quantity</th>
                <th>Username</th>
            </tr>
        </thead>
        <tbody>
        {% for cart_item in cart_items %}
            <tr>
                <td>{{ cart_item.id }}</td>
                <td>{{ cart_item.product.name }}</td>
                <td>{{ cart_item.quantity }}</td>
                <td>{{ cart_item.user.username }}</td>
                <td>
                    <a href="{{ path('cart_item_show', {'id': cart_item.id}) }}">show</a>
                    <a href="{{ path('cart_item_edit', {'id': cart_item.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('cart_item_new') }}">Create new</a>
{% endblock %}
